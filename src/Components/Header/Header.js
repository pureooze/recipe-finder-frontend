import React, { Component } from "react";

class Header extends Component {
  render() {
    return (
      <header className="App-header">
        {/* <h1>{this.props.smallHeader ? "RF" : "Recipe Finder"}</h1> */}
        {/* <h1>RF</h1> */}
      </header>
    );
  }
}

export default Header;
